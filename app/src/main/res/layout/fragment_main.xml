<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

        <!-- Картинка дня -->
        <ImageView
            android:id="@+id/imageViewPictureOfDay"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:contentDescription="@string/picture_of_day" />

        <!-- Кнопка для перехода на AsteroidsByDateFragment -->
        <Button
            android:id="@+id/buttonAsteroidsByDate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/imageViewPictureOfDay"
            android:text="Перейти к астероидам по дате" />

        <!-- Кнопка для перехода на WebViewFragment -->
        <Button
            android:id="@+id/buttonWebView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/buttonAsteroidsByDate"
            android:text="Открыть WebView" />



</androidx.constraintlayout.widget.ConstraintLayout>